// Inference
// Automatically determines the types of varibales, objects based on assign value
let ages = 25;
let names = "Ayan";
console.log('Age: ${ages}');
console.log('Name: ${names}');

// Variable type inference means the programming language automatically deduces the type of a variable from the value assigned to it,
//  without the programmer explicitly specifying the type.

// Array Inference
let fruits = ["Apple", "banana", "Cherry"];
console.log(fruits);

// Funaction return type inference
function adden(a: number, b: number){
    return a + b;

}
console.log(adden(5,10));

// Finally inference means they deduce the return type witouht specifying by the coder.







