// Alias are simply Giving another name to type datatype
type type_alias = number | string | boolean;
let variables: type_alias;
variables = 1;
console.log(variables);
variables = true;
console.log(variables);

